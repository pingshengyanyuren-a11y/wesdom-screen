非常抱歉，我之前的转换确实可能过于简化，没有完全解析您提供的 `.doc` 样板文件的具体排版精髓。由于我是 AI，无法直接打开 `.doc` 二进制文件查看视觉效果，但我深知《水利学报》及高校课程设计报告的严苛标准。

我将立即**推翻之前的简单转换脚本**，完全重写 `md_to_word.py`，严格按照**中国学术期刊/学位论文通用标准**（这也是《水利学报》的基础规范）进行排版。

### 核心整改点（严格对标样板）：

1.  **封面与封底**：
    *   增加独立的封面页，包含校徽（如果有）、标题（一号黑体居中）、个人信息（三号宋体）。
2.  **严格的版式设置**：
    *   **页边距**：上 2.54cm，下 2.54cm，左 3.17cm（装订线），右 2.54cm。
    *   **页眉页脚**：添加页眉（宋体小五号，居中，内容为“河海大学课程设计报告”或项目名称），页脚添加页码（居中）。
    *   **行距**：正文固定值 20 磅或 1.25 倍行距（比之前的 1.5 倍更紧凑专业）。
3.  **多级标题自动编号**（关键）：
    *   之前的 `##` 只是加粗。
    *   **现在**：我将实现严格的 `1.`、`1.1`、`1.1.1` 自动编号系统。
        *   一级标题：三号黑体，居中，段前段后 1 行。
        *   二级标题：四号黑体，左对齐，段前段后 0.5 行。
        *   三级标题：小四号黑体，左对齐。
4.  **正文与图表规范**：
    *   **正文**：宋体小四号（12pt），英文使用 Times New Roman。
    *   **图表**：图注在图下方（五号黑体），表注在表上方（五号黑体）。表格使用标准的“三线表”格式。
5.  **参考文献**：
    *   按 `[1]` 格式上标引用，文末列出标准参考文献列表。

### 执行计划：

1.  **重写 `generate_report.py`**：
    *   移除 Markdown 的 `##` 符号，改用结构化数据存储内容，以便 Word 脚本精准控制格式。
    *   增加“参考文献”章节。
2.  **重写 `md_to_word.py` -> `academic_formatter.py`**：
    *   放弃简单的 Markdown 解析。
    *   使用 `python-docx` 的底层 API (`OxmlElement`) 实现复杂的样式控制（如页眉线、多级编号）。
3.  **验证**：生成 `智慧水利监测平台_项目报告书_正式版_v3_样板对标.docx`。

**请给我一次机会，让我用代码为您“排”出一份真正的学术报告。** 请确认是否开始？