# 项目清理与部署更新计划

根据你的需求，我们确认了以下情况：
1.  **项目结构确认**：你当前的根目录（`c:\Users\ASUS\Desktop\快乐屏`）是完整的 Vue+Python 全栈项目，包含了最新的代码。而`核心代码包`文件夹似乎是一个旧的或备份的副本，**不是**当前运行的项目。
2.  **仓库状态**：远程 GitHub 仓库确实是旧的，本地代码有大量更新。
3.  **部署准备**：项目包含 `vercel.json`，适合在 Vercel 上部署，但目前的配置会拦截 API 请求导致后端失效，需要修复。

为了解决这些问题并顺利部署，我制定了以下计划：

## 1. 清理与配置优化
我们将清理项目中的冗余文件，并修复部署配置。
-   **更新 `.gitignore`**：将 `核心代码包/`、`课设材料/`、以及 `.zip/.rar` 等大文件排除在 Git 版本控制之外，避免仓库体积过大。
-   **修复 `vercel.json`**：当前的配置会将所有请求（包括 API）重定向到前端页面，导致后端失效。我们将添加规则，确保 `/api/*` 请求正确转发到 Python 后端。

## 2. 同步代码到仓库 (更换/更新仓库)
我们将把本地的最新代码推送到远程仓库，覆盖旧版本。
-   **提交更改**：将清理后的代码提交到本地 Git。
-   **推送到 GitHub**：执行 `git push` 将最新代码同步到 `wesdom-screen` 仓库。

## 3. 部署指引
代码推送后，部署流程通常如下：
-   **自动部署**：如果你已经将 Vercel 连接到这个 GitHub 仓库，推送后会自动触发构建。
-   **手动检查**：如果未连接，你需要去 Vercel 控制台导入这个仓库。

---
**确认后，我将执行清理、配置修改和代码推送操作。**